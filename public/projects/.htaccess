RewriteEngine On
RewriteBase /projects/

# Prerendered project folders are real directories — served automatically.
# Missing slug (no matching folder) → SPA; ProjectDetailPage sets noindex.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^.+$ /index.html [L]
