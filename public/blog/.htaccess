RewriteEngine On
RewriteBase /blog/

# Prerendered post folders are real directories — served automatically.
# Missing slug (no matching folder) → SPA; BlogPostPage sets noindex.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^.+$ /index.html [L]
